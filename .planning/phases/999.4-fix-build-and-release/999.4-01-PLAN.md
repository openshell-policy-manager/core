---
phase: 999.4-fix-build-and-release
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .goreleaser.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "goreleaser config is valid for v2+"
    - "Build produces ospm binary without errors"
  artifacts:
    - path: ".goreleaser.yaml"
      provides: "GoReleaser configuration"
      contains: "version: 2"
  key_links:
    - from: ".goreleaser.yaml"
      to: "cmd/ospm/main.go"
      via: "GoReleaser build process"
---

<objective>
Fix GoReleaser v2 compatibility issues so alpha releases work with Homebrew. The main fix is adding `version: 2` to the goreleaser config, which is required by GoReleaser v2+.
</objective>

<context>
@.goreleaser.yaml
@cmd/ospm/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version: 2 to goreleaser config</name>
  <files>.goreleaser.yaml</files>
  <action>
    Add `version: 2` as the first line in the `.goreleaser.yaml` file (before the `project_name` line). This is REQUIRED by GoReleaser v2+ and the config will fail without it.

    The file should start with:
    ```yaml
    version: 2

    project_name: ospm
    ```
  </action>
  <verify>
    <automated>grep -q "^version: 2" .goreleaser.yaml && echo "version: 2 found" || echo "ERROR: version: 2 missing"</automated>
  </verify>
  <done>GoReleaser config has `version: 2` at the top</done>
</task>

<task type="auto">
  <name>Task 2: Test goreleaser build locally</name>
  <files>.goreleaser.yaml, cmd/ospm/main.go</files>
  <action>
    Run the goreleaser build command to verify the configuration works:

    ```bash
    goreleaser build --snapshot --clean
    ```

    This builds the ospm binary from `./cmd/ospm` and creates archives in the `dist/` directory. If successful, the build will produce platform-specific binaries.

    Requirements:
    - Go must be installed (check: go version)
    - goreleaser must be installed (check: goreleaser --version)
  </action>
  <verify>
    <automated>test -d dist && echo "dist/ directory created with build artifacts" || echo "ERROR: dist/ directory not created"</automated>
  </verify>
  <done>goreleaser build succeeds with snapshot release</done>
</task>

</tasks>

<verification>
1. `.goreleaser.yaml` contains `version: 2` at the top
2. `goreleaser build --snapshot --clean` completes without errors
3. `dist/` directory contains build artifacts (binary, checksums, etc.)
</verification>

<success_criteria>
- GoReleaser v2 config is valid
- Build produces ospm binary
- Ready for alpha release testing
</success_criteria>

<output>
After completion, create `.planning/phases/999.4-fix-build-and-release/999.4-01-SUMMARY.md`
</output>
