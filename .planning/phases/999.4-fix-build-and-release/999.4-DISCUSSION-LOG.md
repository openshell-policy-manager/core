# Phase 999.4: fix-build-and-release - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-12
**Phase:** 999.4-fix-build-and-release
**Areas discussed:** GoReleaser version, Build target, Local testing approach

---

## GoReleaser version

| Option | Description | Selected |
|--------|-------------|----------|
| Add version: 2 | Add version: 2 to .goreleaser.yaml (GoReleaser v2+ requires this) | ✓ |
| Downgrade GoReleaser | Check if there's a version 1 config that works | |

**User's choice:** Add version: 2 (Recommended)
**Notes:** GoReleaser v2+ requires version: 2 in config

---

## Build target

| Option | Description | Selected |
|--------|-------------|----------|
| ospm | Main binary is in cmd/ospm/, correct path | ✓ |
| oshell-config | Incorrect path (doesn't exist) | |

**User's choice:** The main binary is `ospm` (in `cmd/ospm/`), not `oshell-config`

---

## Local testing approach

| Option | Description | Selected |
|--------|-------------|----------|
| goreleaser build | Use `goreleaser build --snapshot --clean` to test | ✓ |
| manual go build | Build manually with `go build` command | |

**User's choice:** Use `goreleaser build --snapshot --clean` to test locally before pushing tags

---

## Deferred Ideas

None — discussion stayed within phase scope
