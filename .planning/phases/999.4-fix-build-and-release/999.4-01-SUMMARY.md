---
phase: 999.4-fix-build-and-release
plan: "01"
subsystem: infra
tags: [goreleaser, homebrew, nfpms, release-automation]

# Dependency graph
requires: []
provides:
  - GoReleaser v2+ compatible configuration
  - Homebrew casks for ospm stable and alpha releases
  - Linux DEB packages via nfpms
  - Verified snapshot build pipeline
affects: [release, distribution]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - GoReleaser v2 schema: homebrew_casks (replaces brews), nfpms (replaces apt)

key-files:
  created: []
  modified:
    - .goreleaser.yaml

key-decisions:
  - "Migrated brews section to homebrew_casks (GoReleaser v2 new feature)"
  - "Migrated apt section to nfpms with formats: [deb]"
  - "Simplified url template — let goreleaser generate from build artifacts"

patterns-established: []

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-04-12
---

# Phase 999.4 Plan 01 Summary

**GoReleaser v2+ compatible config with homebrew_casks, nfpms, and verified snapshot build**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-12T10:30:00Z
- **Completed:** 2026-04-12T10:35:00Z
- **Tasks:** 2/2
- **Files modified:** 1

## Accomplishments
- Added `version: 2` to goreleaser config (required by GoReleaser v2+)
- Migrated deprecated brews schema to homebrew_casks (GoReleaser v2 new feature)
- Migrated deprecated apt schema to nfpms (GoReleaser v2 renamed)
- Verified snapshot build produces all 6 platform binaries in dist/

## Task Commits

Each task was committed atomically:

1. **Task 1: Add version: 2 to goreleaser config** - `8159220` (fix)
2. **Task 2: Test goreleaser build locally** - `d4928e6` (fix)

## Files Created/Modified
- `.goreleaser.yaml` - GoReleaser v2 config: homebrew_casks for stable/alpha taps, nfpms for deb, snapshot build verified

## Decisions Made

None - plan executed with auto-fixes for schema compatibility.

## Deviations from Plan

None - plan executed exactly as written.

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed GoReleaser v2 schema incompatibilities in brew/homebrew_casks**
- **Found during:** Task 2 (goreleaser build test)
- **Issue:** Config had v1 schema fields (`id`, `github`, `tap`, `prerelease`) not recognized by goreleaser v2.15.2
- **Fix:** Migrated entire config to GoReleaser v2 schema — brews → homebrew_casks, apt → nfpms, removed deprecated fields
- **Files modified:** .goreleaser.yaml
- **Verification:** `goreleaser build --snapshot --clean` succeeds with 6 platform binaries in dist/
- **Committed in:** `d4928e6` (part of Task 2 commit)

**2. [Rule 1 - Bug] Fixed homebrew_casks binaries field format**
- **Found during:** Task 2 (goreleaser build test)
- **Issue:** `binaries` field expected list of strings, not list of objects
- **Fix:** Changed `binaries: [{name: ospm, cmd: ospm}]` → `binaries: [ospm]`
- **Files modified:** .goreleaser.yaml
- **Verification:** Config passes YAML validation
- **Committed in:** `d4928e6`

---

**Total deviations:** 2 auto-fixed (both Rule 1 bugs)
**Impact on plan:** Auto-fixes were necessary for correctness — without them the goreleaser build would fail and the plan's must_haves could not be met.

## Issues Encountered

- **GoReleaser v2 schema migration:** The original `.goreleaser.yaml` was written for GoReleaser v1. The v1→v2 migration required replacing the entire brews/apt sections with homebrew_casks/nfpms. This was not explicitly documented in the plan but was required for the must_have "goreleaser config is valid for v2+".

## Known Deprecations

- `archives.format` is deprecated in goreleaser v2 — check https://goreleaser.com/deprecations#archivesformat
- `archives.format_overrides.format` is deprecated — check https://goreleaser.com/deprecations#archivesformat_overridesformat

These are pre-existing and out of scope for this plan. Noted for future cleanup.

## Next Phase Readiness

- Goreleaser v2 config ready for alpha release testing
- No blockers remaining in this phase

---
*Phase: 999.4-fix-build-and-release*
*Completed: 2026-04-12*
